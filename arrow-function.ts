// When declare normal function
function add(num1: number, num2: number): number {
    return num1 + num2;
}

// When we declare arrow function 
const add2 = (num3: number, num4: number): number => num3 + num4;
// multiline
const doMath = (x: number, y: number): void => {

}